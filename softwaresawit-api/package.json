{
  "name": "softwaresawit-api",
  "version": "1.0.0",
  "description": "Cloudflare Workers API for SoftwareSawit - Offline-first multi-device sync",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests configured yet\" && exit 0"
  },
  "dependencies": {
    "hono": "^4.0.0",
    "@hono/zod-validator": "^0.2.0",
    "zod": "^3.22.4",
    "jose": "^5.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240117.0",
    "typescript": "^5.3.3",
    "wrangler": "^3.0.0"
  }
}
